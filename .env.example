# Puppeteer mode: "local" (default) or "serverless"
# - local: use bundled puppeteer + Chrome (local dev, Node server)
# - serverless: use puppeteer-core + @sparticuz/chromium (Vercel, serverless)
PDF_BROWSER=local

# Optional: request body size limit in bytes (default handled by Next.js)
# PDF_MAX_BODY_SIZE=2097152

# Optional: timeout for PDF generation in ms
# PDF_TIMEOUT_MS=30000
